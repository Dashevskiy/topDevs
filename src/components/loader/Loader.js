import React from "react";
import './index.css';

export const Loader = () => {
    return (
        <div className="container">
            <div className="loader">Loading...</div>
        </div>
    )
}